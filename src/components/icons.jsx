import React from "react";

export const PresetsIcon = ({ className }) => (
  <svg className={className} /* ... SVG props ... */>
    {/* SVG content for PresetsIcon */}
  </svg>
);

export const ManualIcon = ({ className }) => (
  <svg className={className} /* ... SVG props ... */>
    {/* SVG content for ManualIcon */}
  </svg>
);

export const DocumentScannerIcon = ({ className }) => (
  <svg className={className} /* ... SVG props ... */>
    {/* SVG content for DocumentScannerIcon */}
  </svg>
);
